import os

class Paths:
    RESOURCES = "resources"
    AUDIO = os.path.join(RESOURCES, "audio")
    FONTS = os.path.join(RESOURCES, "fonts")
    CHARACTERS = os.path.join(RESOURCES, "characters")
    UI = os.path.join(RESOURCES, "ui")
    SAVES = "saves"
    DATA = "data" 